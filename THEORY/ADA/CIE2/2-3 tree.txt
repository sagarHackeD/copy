A 2-3 tree is a balanced search tree where each internal node can have either two children (2-node) with one data element or three children (3-node) with two data elements. This structure ensures that all leaf nodes are at the same level, maintaining sorted data and allowing efficient operations with a worst-case time complexity of O(log n) for search, insert, and delete operations.