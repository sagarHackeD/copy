>CREATE TABLE employee (empno INT,ename VARCHAR(10),job VARCHAR(10),mgr_no INT,sal DECIMAL(10,2),commission DECIMAL(10,2));
>CREATE USER 'dbms'@'localhost' IDENTIFIED BY 'dbms403';
>GRANT ALL PRIVILEGES ON *.* TO 'dbms'@'localhost';
>FLUSH PRIVILEGES;
>SELECT User, Host FROM mysql.user;
>SET autocommit = 0;
>INSERT INTO employee VALUES (1, 'Ram', 'Manager', 101, 50000, 5000);
>SELECT * FROM employee;
>ROLLBACK;
>SELECT * FROM employee;
>ALTER TABLE employee ADD PRIMARY KEY (empno);
>ALTER TABLE employee MODIFY ename VARCHAR(10) NOT NULL;
>insert into employee values(106,NULL,'HR',5509,456 ,80);