>CREATE TABLE Employee (E_id INTEGER PRIMARY KEY,E_name VARCHAR(100),Age INTEGER,Salary DECIMAL(10,2));
>INSERT INTO Employee VALUES (1,'Braham Kumar',30,50000);
>SELECT COUNT(E_name) from Employee;
>SELECT max(age) from Employee;
>SELECT min(age) from Employee;
>SELECT E_NAME,SALARY FROM EMPLOYEE ORDER BY SALARY ASC;
>INSERT INTO Employee VALUES (6,'Kumar',30,50000);
>SELECT Age,SUM(SALARY) AS "TOTAL SALARY" FROM EMPLOYEE GROUP BY AGE;